name: Build and Test

on:
  push:
    tags-ignore:
      - '*'
    branches:
      - '*'

jobs:
  buid-and-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:alpine
        ports:
          - 5432:5432
        env:
          TZ: Asia/Tokyo
          POSTGRES_USER: toyokumo
          POSTGRES_PASSWORD: commons
          POSTGRES_DB: toyokumo
      redis:
        image: redis:alpine
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-java@v1
        with:
          java-version: '11'
          java-package: jdk
          architecture: x64

      - uses: DeLaGuardo/setup-clojure@master
        with:
          lein: latest

      - name: Show versions
        run: |
          java -version
          lein version

      - name: Cache dependencies
        uses: actions/cache@v2
        with:
          path: ~/.m2
          key: v2-dependencies-${{ hashFiles('project.clj') }}
          restore-keys: |
            v2-dependencies-${{ hashFiles('project.clj') }}
            v2-dependencies-

      - name: Fetch dependencies
        run: lein deps

      - name: Test
        run: lein test
